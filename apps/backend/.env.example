PORT=3000

# Default mode is postgres. Use memory only for local mock mode.
DB_MODE=postgres

# Option 1: discrete connection fields
DB_HOST=localhost
DB_PORT=5432
DB_USER=finlab
DB_PASSWORD=finlab
DB_NAME=finlab
DB_INIT_MAX_ATTEMPTS=30
DB_INIT_RETRY_DELAY_MS=1000

# Option 2: full URL (takes precedence over DB_* fields)
# DATABASE_URL=postgresql://finlab:finlab@localhost:5432/finlab

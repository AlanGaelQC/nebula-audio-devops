# Ansible (demo)

Objetivo: practicar automatización de configuración (T3/T8).
En laboratorio local se evalúa:
- estructura de inventario
- playbook idempotente
- ejecución contra localhost o VM de prueba

Ejemplo: `ansible-playbook -i inventory.ini playbooks/00_ping.yml`
